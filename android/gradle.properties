org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Disable incremental Kotlin to avoid cross-drive path issues (pub cache on C:, project on E:)
kotlin.incremental=false
# Use system aapt2 (qemu-wrapped) instead of the x86-64 Maven binary on aarch64 hosts
android.aapt2FromMavenOverride=/usr/lib/android-sdk/build-tools/36.0.0/aapt2
